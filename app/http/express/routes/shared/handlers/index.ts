/**
 * Shared handler utilities for route architecture
 *
 * These provide the foundation for the new class-based handler pattern
 * following the improvements.md specification.
 */

export { BaseHandler } from "./BaseHandler";

// Export types for convenience
export type { ApiError } from "../responses";
